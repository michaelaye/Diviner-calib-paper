\section{General idea}
a.k.a. the elevator pitch:

In regular intervalls, Diviner looks at space, to define the counts for zero radiation ($\sigma_{space}$). Around the same time the instrument also points at an internal blackbody source at a measured temperature ($\sigma_{sensor}$, $\sigma_{placement}).